#include <stdio.h>
#include <stdlib.h>
int mod(int, int);
int main() {
	int x = 17, y = 5;
    printf("%d %% %d = %d\n", x, y, mod( x, y));
    return 0;
}
int mod(int x, int y)
{
    return x % y;
}
