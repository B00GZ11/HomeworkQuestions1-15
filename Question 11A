#include <stdio.h>
int main(void) {
    int x = 1;
    while (!((x % 3 == 2) && (x % 5 == 3) && (x % 7 == 2))) {
        x++;
    }
    printf("The smallest integer satisfying the Sunzi problem is: %d\n", x);
    return 0;
}
