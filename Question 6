#include <stdio.h>
#include <stdlib.h>
int power(int, int);
int main(void){
	int x = 5, n = 3;
	printf("%d to the %d power is %d\n", x, n, power(x, n));
	system("pause");
	return 0;
}
int power(int base, int n){
	int i;
	int pow = 1;
	for ( i = 1; i <= n; i++)
		pow *= base;
	return pow;
}
