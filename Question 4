#include <stdio.h>
#include <stdlib.h>
double square(double);
int main(void)
{
	printf("square( 4.0 )= %f.\n", square( 4.0 ));
	system("pause");
	return 0;
}

double square(double x)
{
	int i;
	double pow = 1.0;
	for (i = 1; i <= 2; i++)
        pow = pow * x;
	return pow;
}
