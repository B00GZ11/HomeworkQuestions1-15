#include <stdio.h>
#include <stdlib.h>
int cub(int);
int main(void)
{
	printf("2 cubed is: %d.\n", cub( 2 ));
	system("pause");
	return 0;
}

int cub(int x)
{
	int i;
	double pow = 1.0;
	for (i = 1; i <= 3; i++)
        pow = pow * x;
	return pow;
}
